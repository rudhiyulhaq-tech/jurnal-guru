import React,{useEffect} from 'react';
import {BookOpen,X,ChevronRight} from 'lucide-react';
export const statuses={H:'Hadir',S:'Sakit',I:'Izin',A:'Alpa'};
export const journalFields=[['cp','Capaian pembelajaran (CP)'],['tp','Tujuan pembelajaran (TP)'],['criteria','Kriteria ketercapaian tujuan pembelajaran'],['understand','Memahami'],['apply','Mengaplikasi'],['reflect','Merefleksi'],['principles','Penerapan berkesadaran, bermakna, menggembirakan'],['reflection','Refleksi guru'],['followup','Tindak lanjut / diferensiasi']];
export function Field({label,children,...props}){return <label className="field"><span>{label}</span>{children||<input {...props}/>}</label>}
export function Empty({title,body,action}){return <div className="empty"><BookOpen size={30}/><h3>{title}</h3><p>{body}</p>{action}</div>}
export function Modal({title,children,onClose}){useEffect(()=>{const handler=e=>{if(e.key==='Escape')onClose();if(e.key==='Tab'){const items=[...document.querySelector('.modal').querySelectorAll('button,input,select,textarea,a')].filter(e=>!e.disabled);const first=items[0],last=items.at(-1);if(e.shiftKey&&document.activeElement===first){last?.focus();e.preventDefault()}else if(!e.shiftKey&&document.activeElement===last){first?.focus();e.preventDefault()}}};document.addEventListener('keydown',handler);const old=document.body.style.overflow,active=document.activeElement;document.body.style.overflow='hidden';document.querySelector('.modal input, .modal button')?.focus();return()=>{document.removeEventListener('keydown',handler);document.body.style.overflow=old;active?.focus()}},[]);return <div className="backdrop"><section className="modal" role="dialog" aria-modal="true" aria-label={title}><header><h2>{title}</h2><button className="icon" onClick={onClose} aria-label="Tutup"><X/></button></header>{children}</section></div>}
export function MeetingRow({m,onClick}){const count=Object.values(m.attendance||{}).filter(s=>s.status).length;return <button className="meeting-row" onClick={onClick}><div className="date-block"><strong>{m.date.slice(8)}</strong><small>{new Date(m.date+'T12:00:00').toLocaleDateString('id-ID',{month:'short'})}</small></div><div className="meeting-info"><strong>{m.topic}</strong><small>{m.time} · {m.duration} JP · {count}/{m.roster.length} absen terisi</small></div><span className={'badge '+(count===m.roster.length?'good':'')}>{count===m.roster.length?'Absen lengkap':'Perlu diisi'}</span><ChevronRight size={18}/></button>}
